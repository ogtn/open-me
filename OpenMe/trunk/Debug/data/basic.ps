#version 120

// inputs

varying vec3 color;
varying vec3 normal;
varying vec3 position;

void main(void)
{
	float f = dot(normalize(position), normal);
	gl_FragColor = vec4(color * f * 10, 1.f);
}
